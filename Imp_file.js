/* Импортируем свойство объекта в данный файл*/

require (`./Exp_file.js`);

console.log(`У меня есть ` + apple.device1 + ` с памятью ` + apple.memory1);

console.log(`Но я хочу купить ` + apple.device2 + ` с цветом ` + apple.color2);